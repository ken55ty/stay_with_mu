<div class="container mx-auto">
  <div class="bg-base-300 py-5 px-3">
    <div class="flex">
      <p class="mr-3 rounded-full">
        <%= image_tag "kkrn_icon_user_1.png", size: "80x80"%>
      </p>
      <p class="p-3 text-2xl">
        <%= @user.name%>
      </p>
    </div>

    <div>
      <%= @user.profile %>
      <% if logged_in? && current_user == @user %>
        <%= link_to "編集", edit_user_path(@user), class: "btn btn-sm btn-secondary float-right"%>
      <% end %>
    </div>

  </div>
</div>

<div class="container mx-auto mt-10">
  <div class="bg-base-300">
    <div role="tablist" class="tabs tabs-lifted">
    <input type="radio" name="my_tabs_2" role="tab" class="tab" aria-label="つくったMU" checked/>
    <div role="tabpanel" class="tab-content bg-base-100 border-base-300 rounded-box p-6">つくったMU</div>

    <input type="radio" name="my_tabs_2" role="tab" class="tab" aria-label="いいねしたMU" />
    <div role="tabpanel" class="tab-content bg-base-100 border-base-300 rounded-box p-6">準備中です。
    </div>
  </div>
</div>